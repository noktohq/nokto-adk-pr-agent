# Local development configuration
# For deployment, these are set via Terraform / the deploy command.

# Vertex AI Configuration (default)
GOOGLE_GENAI_USE_VERTEXAI=true
GOOGLE_CLOUD_PROJECT=your-gcp-project-id
GOOGLE_CLOUD_LOCATION=global

# Alternatively, for Gemini API via Google AI Studio,
# comment out the three lines above and uncomment:
# GEMINI_API_KEY=your-api-key-here

# Optional authenticated GitHub reads. When set, an explicit allowlist is required.
# Store the token in Secret Manager for Cloud Run; never commit it.
# GITHUB_TOKEN=github-token-from-secret-manager
# GITHUB_ALLOWED_REPOSITORIES=noktohq/nokto-agent-orchestrator
